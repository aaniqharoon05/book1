# book Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-12

## Active Technologies
- Python 3.x + Cohere (for embeddings), Qdrant client (for vector database), requests/httpx (for web content fetching), BeautifulSoup/lxml (for HTML parsing from Docusaurus site), uv (for project initialization) (001-rag-pipeline-implementation)
- Qdrant vector database ("chatbot_embedding" collection) (001-rag-pipeline-implementation)

- Python 3.x for code examples (rclpy), Node.js (latest LTS) for Docusaurus, Markdown/MDX for content, TypeScript/React for UI components. (001-book-structure-spec)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.x for code examples (rclpy), Node.js (latest LTS) for Docusaurus, Markdown/MDX for content, TypeScript/React for UI components.: Follow standard conventions

## Recent Changes
- 001-rag-pipeline-implementation: Added Python 3.x + Cohere (for embeddings), Qdrant client (for vector database), requests/httpx (for web content fetching), BeautifulSoup/lxml (for HTML parsing from Docusaurus site), uv (for project initialization)

- 001-book-structure-spec: Added Python 3.x for code examples (rclpy), Node.js (latest LTS) for Docusaurus, Markdown/MDX for content, TypeScript/React for UI components.

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
